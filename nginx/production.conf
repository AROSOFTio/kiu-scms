server {
    listen 80;
    server_name scms.arosoft.io;

    # Redirect HTTP to HTTPS (uncomment when SSL is ready)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://frontend:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /api {
        proxy_pass http://backend:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Internal port mapping fallback or additional logic if needed
}

# HTTPS Configuration (uncomment when certificates are obtained via Certbot)
# server {
#     listen 443 ssl;
#     server_name scms.arosoft.io;
#
#     ssl_certificate /etc/letsencrypt/live/scms.arosoft.io/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/scms.arosoft.io/privkey.pem;
#
#     location / {
#         proxy_pass http://frontend:80;
#         ...
#     }
#
#     location /api {
#         proxy_pass http://backend:5000;
#         ...
#     }
# }
