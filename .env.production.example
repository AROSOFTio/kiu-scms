# ======================================================== #
#        SCMS PRODUCTION DEPLOYMENT ENVIRONMENT FILE       #
# ======================================================== #
# Instructions:
# Rename this file to `.env` inside the root directory
# before running `docker-compose -f docker-compose.prod.yml up -d`

# ----------------- DATABASE CONFIGURATION -----------------
DB_ROOT_PASSWORD=ChangeThisSecurePassword!2026
DB_NAME=scms_db
DB_USER=kiu_user
DB_PASSWORD=SecurePasswordForKiuUser99!

# ------------------ BACKEND CONFIGURATION -----------------
# Ensure JWT_SECRET is a long, unguessable string
JWT_SECRET=YOUR_SUPER_SECRET_PRODUCTION_KEY_MIN_32_CHARS
NODE_ENV=production

# ----------------- FRONTEND CONFIGURATION -----------------
# The external URL your React application uses to contact the backend.
VITE_API_URL=https://scms.arosoft.io/api

# ----------------- OPTIONAL: NOTIFICATIONS ----------------
EMAIL_SERVICE=
EMAIL_USER=
EMAIL_PASS=
